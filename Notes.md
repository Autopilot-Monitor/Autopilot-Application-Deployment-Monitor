# Download ookla speed test in script and extract the exe (don't want to store the exe in our repo)
# Combine the pre-app deployment monitor with the app deployment monitor and call the 1 script during pre-provisioning
# instructions for shiftf10 or a way to download shift f10 and create the configuration on the fly
# test PSADT version 4.1 to see if gui displays during autopilot (expected in version 4.2)
# if test is not successful, create instructions for including the prerequisite serviceui.exe in the bits
